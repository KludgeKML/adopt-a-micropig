<%= form_with(model: micropig) do |form| %>
  <% if micropig.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(micropig.errors.count, "error") %> prohibited this micropig from being saved:</h2>

      <ul>
        <% micropig.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :age, style: "display: block" %>
    <%= form.number_field :age %>
  </div>

  <div>
    <%= form.label :notes, style: "display: block" %>
    <%= form.text_area :notes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
